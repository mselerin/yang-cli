import { SplopDirective } from './<%=dasherize_name%>.directive';

describe('<%=classify_name%>Directive', () => {
  it('should create an instance', () => {
    const directive = new <%=classify_name%>Directive();
    expect(directive).toBeTruthy();
  });
});
